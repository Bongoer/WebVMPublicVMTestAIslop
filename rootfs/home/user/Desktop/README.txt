Welcome to the XFCE Browser VM.

Desktop login is automatic.

User:
  username: user
  password: webvm

Administrator:
  username: root
  password: root

For network access, use the website's "Start + Network" button.
When the Tailscale login link appears, open it and approve the connection.

Use curl or wget instead of ping.
Every website visitor gets a separate VM stored in that visitor's browser.
