// Replaced by the GitHub Actions workflow with the generated image name.
export const diskImageUrl = IMAGE_URL;

// WebVM's GitHub Pages split-image backend.
export const diskImageType = "github";

export const printIntro = false;
export const needsDisplay = true;

// OpenRC starts Xorg, LightDM and the XFCE session.
export const cmd = "/sbin/init";
export const args = [];

export const opts = {
  uid: 0,
  gid: 0
};
